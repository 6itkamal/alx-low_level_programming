#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	/*srand(time(0));*/

		/*n = rand() - RAND_MAX / 2;*/

			/* your code goes there */
		int d;

		for (d = '0'; d <= 10; d++)
		{
			putchar(d);
			if
				(d != '9');
			{
				putchar (',');
					putchar (' ');
			}
		}
		putchar('\n');
		return (0);
}
